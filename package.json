{
  "name": "@f/hsv-to-rgb",
  "description": "Convert an HSV color to RGB",
  "repository": "git://github.com/micro-js/hsv-to-rgb.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
